# A股量化分析网站：可行性分析与执行任务书

> 分析基线：`A股量化分析网站_AI项目生成提示词.md`  
> 编制日期：2026-08-26  
> 文档用途：作为项目实施、任务分派、阶段验收和风险控制的唯一执行入口。

## 1. 结论

项目**有条件可行**，适合实现为个人研究用途的 A 股量化工作台，但不能原样、无验证地一次性实现全部需求。

可以稳定落地的部分：

- React + Cloudflare Workers + D1 的查询型网站。
- 最近完整交易日展示、缺口检测、幂等补数、同步状态和失败恢复。
- 日行情、技术指标、横截面评分、自定义筛选、市场信号、个股详情和每日候选。
- 从上线日起持续保存候选，并在未来交易日到达后计算真实历史表现。

必须先验证或调整的部分：

- 单个免费 D1 数据库不适合直接承载全 A 股 10 年全部行情、估值和评分历史；Free 计划尤其不满足目标容量。
- AKShare 主要封装第三方公开网站，接口可能变更或被限流，不能把“接口目前存在”当成长期 SLA。
- 仅有 `financial_latest` 无法进行无前视偏差的历史评分；必须保存财报报告期、公告/披露日和版本。
- 仅从当前股票列表回算历史，会产生幸存者偏差；严谨历史验证还需要历史股票池、退市股票和当时可见数据。
- 免费数据对历史估值、历史行业/概念归属和历史资金流的覆盖不一定完整，必须在 Phase 0 实测。
- “网页打开自动触发同步”必须与交易日历、原子锁、租约过期和 GitHub Actions 并发锁一起实现。

因此，本任务书将 V1 定义为：**5 年行情底座 + 当前全市场分析 + 上线后无前视的候选追踪 + 可恢复的每日数据链路**。10 年历史、严谨的历史基本面回测和完整历史资金流属于验证通过后的 V1.1/V2。

## 2. 可行性矩阵

| 能力 | 结论 | 主要条件或限制 |
| --- | --- | --- |
| 前端 Dashboard、筛选器、个股详情 | 可行 | API 契约和真实数据先完成，UI 不先使用假数据定型 |
| 全市场日行情与技术指标 | 可行 | 先验证历史接口速度、复权口径、停牌和北交所覆盖 |
| 七维横截面评分 | 可行 | 明确定义缺失值、异常值、方向、权重归一化和配置版本 |
| 最近交易日判断 | 可行 | 增加 `trade_calendar`，Worker 不用“工作日”猜交易日 |
| 多日缺口自动补齐 | 有条件可行 | 历史按日期批量能力不足时，逐股抓取可能超过 Action 时限 |
| GitHub Actions 按需同步 | 可行 | Fine-grained token 仅给目标仓库 `Actions: write`，工作流位于默认分支 |
| D1 保存 5 年历史 | 有条件可行 | 使用 Workers Paid、多 D1 分库，并先做容量实测 |
| 单个 Free D1 保存 10 年完整数据 | 不可行 | Free 单库上限 500 MB；初始化写入也会触发每日写限制 |
| 5–10 年历史财务评分 | 高风险 | 必须取得公告日明确的 point-in-time 财务数据 |
| 严谨的历史策略回测 | 当前条件不足 | 还需历史股票池、退市股、公司行为和当时可见财务数据 |
| 行业/概念资金流 | 有条件可行 | 供应商衍生口径，非交易所官方；历史覆盖不稳定 |
| 从上线日起的候选表现验证 | 可行 | 候选快照不可覆盖，收益区间和基准口径必须冻结 |

## 3. 平台与数据约束

### 3.1 D1 容量

按 5,500 只股票、每年 244 个交易日估算：

- 5 年行情约 671 万行；行情和每日评分合计约 1,342 万行。
- 10 年行情约 1,342 万行；行情和每日评分合计约 2,684 万行。
- 再加入每日估值、索引和中间状态后，10 年数据很可能接近或超过单库 10 GB 硬上限。

Cloudflare 当前官方限制为：Free 单库 500 MB、Paid 单库 10 GB；Free 每日 500 万行读取、10 万行写入。初始化和索引写入也计费。因此：

1. 本项目部署基线采用 Workers Paid。
2. 使用 `core`、`market_history`、`analytics_history` 三个 D1 数据库，不依赖跨库 JOIN。
3. Phase 0 用真实样本测量 SQLite/D1 实际字节数，再决定保留 5 年还是扩展到 10 年。
4. 单库预测容量达到 7 GB 时必须分片或把冷历史归档到 R2/Parquet，不允许等到 10 GB 才处理。

参考：[D1 limits](https://developers.cloudflare.com/d1/platform/limits/)、[D1 pricing](https://developers.cloudflare.com/d1/platform/pricing/)、[D1 import/export](https://developers.cloudflare.com/d1/best-practices/import-export-data/)。

### 3.2 AKShare 风险

AKShare 官方明确说明数据仅用于学术研究、不构成投资建议，且部分接口未来可能移除。它适合作为个人研究工具的数据适配层，不适合作为无备用方案的生产数据 SLA。

必须做到：

- 所有源接口只出现在 `providers/` 内。
- Provider 输出统一英文列名、单位和空值语义。
- 每个 Provider 有能力清单，例如是否支持“按日期取全市场”、历史估值、公告日财务、北交所和退市股。
- 保存 `source`、`source_version`、`fetched_at`、原始行数和内容摘要。
- 每日 live smoke test 与本地 fixture contract test 分开。
- 资金流缺失不阻塞核心行情，行情不完整则禁止生成当日候选。

参考：[AKShare 特别说明](https://akshare.akfamily.xyz/special.html)、[AKShare 股票数据](https://akshare.akfamily.xyz/data/stock/stock.html)。

### 3.3 GitHub Actions

GitHub-hosted runner 单个 job 最长运行 6 小时。私人仓库 GitHub Free 当前包含每月 2,000 分钟。首次全历史抓取不应依赖一个 Action job 完成，应在本地执行可断点初始化；Actions 只负责每日增量和修复。

推荐同步触发方式：

1. 工作日 17:00 Asia/Shanghai 定时同步，减少累积缺口。
2. 网站打开时只做幂等缺口检查，缺口存在且未加锁时触发 `workflow_dispatch`。
3. 每周执行完整性巡检。
4. Action 启动后自行重新计算缺口，不信任前端传入日期。

参考：[Actions limits](https://docs.github.com/en/actions/reference/limits)、[Actions usage](https://docs.github.com/en/actions/concepts/billing-and-usage)、[workflow dispatch API](https://docs.github.com/en/rest/actions/workflows)。

## 4. 原需求必须修订的设计点

### 4.1 补充数据表

原文缺少以下关键实体，实施时必须增加：

- `trade_calendar`：交易日、是否开市、市场、来源、更新时间。
- `financial_reports`：报告期、公告日、修订版本和财务字段，供 as-of JOIN。
- `daily_valuation`：每日市值、PE/PB/PS、股息率及来源；如果无法回填则记录可用起始日。
- `index_quotes`：上证、深证、创业板、沪深 300 的行情和基准收益。
- `sectors`、`sector_memberships`：板块及成分股多对多关系；可用时保存有效期。
- `daily_signal_members`：每日信号对应股票，不在请求时重复计算全市场。
- `sync_runs`：每次同步的审计历史，不能只保留一行 `sync_state`。
- `dataset_import_status`：按日期、数据集记录 expected/actual/status/error，替代持续扩张的布尔列。
- `config_versions`：因子、评分、策略和信号配置版本。

`daily_import_status` 可以保留为查询视图，但不作为唯一事实表。

### 4.2 防止前视偏差和幸存者偏差

历史某交易日只能使用该日收盘前已经公开的数据：

```text
financial_reports.announce_date <= trade_date
```

同一报告期有修订时，按当时已发布的最新版本选择。无法取得可靠公告日时，该因子不得进入历史评分，只能从数据上线日开始使用。

历史股票池不能只使用今天仍上市的股票。Phase 0 若无法取得历史上市/退市状态，V1 历史回算必须标记为“当前股票池回顾性研究”，不得展示为严谨回测；正式表现统计从上线后的不可变候选开始。

### 4.3 价格口径

必须在数据字典中冻结以下口径：

- K 线展示：不复权 OHLC。
- 技术指标：统一复权序列，不能混用不复权和前/后复权价格。
- 候选表现：默认信号生成后的下一交易日开盘买入，持有至第 N 个交易日收盘；基准使用完全相同区间。
- 如同时展示“信号日收盘到未来收盘”，必须明确标为研究口径，不能与可执行口径混合。
- 停牌、涨跌停无法成交和除权除息必须有质量标记。

### 4.4 同步锁

锁必须是数据库原子条件更新，并带租约：

```text
idle/failed/success -> pending
pending -> running
lease_expires_at < now -> 可接管
```

仅当条件更新影响 1 行时才允许 dispatch。工作流再使用 GitHub Actions `concurrency` 做第二层互斥。公开调用者不能指定目标日期、仓库或工作流，只能请求服务器自行检查真实缺口。

### 4.5 设置语义

本地 `localStorage` 设置不能悄悄改变服务器生成的每日候选。V1 分为：

- 本地视图偏好：主题、表格列、筛选模板，可直接保存到浏览器。
- 本地权重试算：只重算已存在的七维分数，并明确显示“个人试算”。
- 系统每日评分：只读取受版本控制的服务器配置，修改后由后台任务生成新 `config_version`。

若需要在网页直接修改系统配置，必须另加 Cloudflare Access 或管理鉴权，不能开放匿名写接口。

## 5. V1 范围冻结

### 5.1 V1 必须交付

- 5 年日行情初始化，实际保留年限以容量 Gate 为准。
- 上证、深证、创业板、沪深 300 基准数据。
- 交易日历、股票基础信息和最新状态。
- 技术、动量、量价、位置和风险类因子。
- 能取得可靠公告日的成长、质量因子；不能取得的因子显示 unavailable。
- 当前估值因子；历史估值分位仅在来源覆盖足够时开启。
- 七维评分、5 类每日候选、结构化理由和配置版本。
- Dashboard、热力图、板块、信号、筛选器、个股详情。
- 定时同步、打开网站补漏、同步锁、完整性状态和恢复流程。
- 从上线日起保存不可变候选，计算 1/5/20/60 日表现和基准超额。
- 本地运行、自动测试、Cloudflare 部署和运维说明。

### 5.2 V1 可降级项

- 历史资金流：从上线日起积累。
- 历史概念归属：无有效期数据时只展示当前归属。
- 历史财务评分：公告日不可靠时只从验证通过日期启用。
- 10 年历史：5 年容量与抓取验证通过后再扩展。
- MFE/MAE：先完成基本收益验证，再加入。

### 5.3 明确不在 V1

- 自动交易、下单和仓位管理。
- 实时 Level-2。
- 将评分解释为上涨概率。
- 在缺少历史退市股和 point-in-time 财务时宣称“无偏回测”。
- 商业化或向第三方分发 AKShare 数据。

## 6. 目标架构

```text
Browser
  -> Cloudflare Worker API
       -> D1 core
       -> D1 market_history
       -> D1 analytics_history
       -> GitHub Actions dispatch（仅在原子锁成功后）

GitHub Actions / local Python pipeline
  -> DataProvider(s)
  -> local staging SQLite/Parquet
  -> validation + factors + scores + picks
  -> D1 REST API / Wrangler 分批 UPSERT
  -> sync status callback
```

建议目录：

```text
/
├── frontend/
├── worker/
├── packages/
│   └── contracts/          # API schema、共享类型、错误码
├── pipeline/
│   ├── providers/
│   ├── jobs/
│   ├── storage/
│   └── calculations/
├── config/
├── migrations/
│   ├── core/
│   ├── market_history/
│   └── analytics_history/
├── tests/
│   ├── fixtures/
│   ├── integration/
│   └── e2e/
├── docs/
└── .github/workflows/
```

实施选型：

- Node 使用当前维护中的 LTS 并在仓库固定版本；包管理器使用 pnpm workspace。
- Python 3.12，依赖与锁文件使用 uv，数据处理先使用 Pandas 以减少 AKShare 转换成本。
- Worker 使用 TypeScript、Hono、Zod 和 D1 prepared statements。
- 前端使用 React、TypeScript、Vite、ECharts、CSS Modules。
- 测试使用 Vitest、React Testing Library、Playwright、Pytest 和 Ruff。

## 7. 逐步执行任务

每个任务必须按编号执行。前一 Gate 未通过时，不得进入下一 Phase。

### Phase 0：可行性验证与口径冻结

#### T0.1 建立决策记录

**工作**：创建 ADR，确认个人非商业研究用途、Workers Paid 基线、5 年初始保留期、三库方案和 V1 降级范围。  
**产物**：`docs/adr/0001-scope-and-storage.md`。  
**验收**：所有未决项有 owner、截止时间和默认降级方案。

#### T0.2 数据源能力探针

**工作**：用至少 30 个代表性证券验证沪市、深市、创业板、科创板、北交所、ST、停牌、新股和退市样本；测试股票列表、交易日历、日行情、指数、财务公告日、估值、行业、概念和资金流。  
**产物**：Provider capability matrix、原始字段样本、单位映射和错误分类。  
**验收**：核心行情最近 20 个完整交易日覆盖率不低于 99%；所有字段都能追溯到 source；失败可重试且不会返回伪成功。

#### T0.3 缺口补齐性能验证

**工作**：分别模拟缺 1 日、5 日和 20 日，测量调用次数、耗时、失败率和重试后耗时。  
**产物**：`docs/data-provider-benchmark.md`。  
**验收**：5 日缺口在 90 分钟内可完成；否则必须增加按日期批量 Provider、定时日更或分片 job。

#### T0.4 D1 容量与导入 POC

**工作**：导入 100 只股票 5 年真实行情、估值和评分样例，创建预期索引并测量数据库字节数、写入时间和典型查询 rows_read。  
**产物**：容量外推表和分库决定。  
**验收**：任何单库 5 年预测容量低于 7 GB；典型详情和筛选查询走索引；导入可从中断点继续。

#### T0.5 数据字典和因子公式冻结

**工作**：逐项定义单位、空值、异常值、复权、窗口最小样本、percentile ties、winsorize、负 PE、停牌、上市天数、涨跌停和收益口径。  
**产物**：`docs/data-dictionary.md`、`docs/factor-spec.md`。  
**验收**：每个 V1 因子有公式、输入字段、方向、缺失规则和至少 3 个示例。

**Gate 0**：T0.2–T0.5 全部通过。若 point-in-time 财务失败，历史基本面评分自动降级为上线后启用；若容量失败，缩短 D1 热数据年限或启用 R2 冷归档。

### Phase 1：仓库与质量基线

#### T1.1 初始化 monorepo

创建 `frontend`、`worker`、`packages/contracts`、`pipeline`、`config`、`migrations` 和 `tests`；固定 Node/Python/pnpm/uv 版本。

#### T1.2 建立统一命令

根目录至少提供：

```text
pnpm lint
pnpm typecheck
pnpm test
pnpm build
pnpm test:e2e
uv run ruff check .
uv run pytest
```

#### T1.3 建立 CI

Pull request 运行 lint、typecheck、unit test 和 build；live provider 测试不进入普通 PR，避免第三方波动导致主 CI 随机失败。

#### T1.4 环境与密钥模板

创建 `.env.example`，只列变量名和说明；配置 secret scanning 与 `.gitignore`。任何真实 Token 不得写入日志、fixture 或前端构建。

**Gate 1**：空骨架的全部 lint/test/build 通过，README 能在干净目录复现。

### Phase 2：数据库与 API 契约

#### T2.1 设计三库 migration

- `core`：股票、交易日历、财务、板块关系、最新快照、同步和版本。
- `market_history`：日行情、每日估值、指数行情、板块日数据。
- `analytics_history`：每日评分、候选、信号成员和表现。

所有金额统一元，比例统一明确为小数或百分数，不允许混用；日期使用 `YYYY-MM-DD`，时间存 UTC ISO-8601。

#### T2.2 创建约束和索引

至少验证 `(trade_date, code)` 唯一约束、按 code 的历史索引、按日期排名索引、`stock_latest` 可筛选字段索引。每个索引必须对应具体查询，不建立“可能有用”的大量索引。

#### T2.3 定义 API contract

统一响应：`data`、`meta.dataDate`、`meta.generatedAt`、`meta.sourceStatus`、`error.code`。列表 API 支持 limit/cursor，日期为空时使用最近完整日，而不是系统当天。

#### T2.4 实现 Worker 基础层

完成路由、Zod 校验、统一错误、CORS、请求 ID、缓存头和 D1 repository；禁止 route 直接拼接 SQL。

**Gate 2**：三库 migration 可本地重复执行；contract test 通过；恶意 field/op/sort 输入被拒绝。

### Phase 3：数据链路

#### T3.1 实现 Provider 协议

协议返回规范化模型和 `FetchMetadata`，支持 timeout、指数退避、jitter、限速、checkpoint 和 partial failure。实现 `AkShareProvider`；仅在 Gate 0 需要时增加第二 Provider。

#### T3.2 实现本地 staging

先写本地 SQLite/Parquet，再校验并发布 D1。抓取失败不得直接污染远端完整日。staging 文件进入 `.gitignore`。

#### T3.3 初始化股票、日历和指数

交易日历至少预填当前年及下一年；股票代码保持字符串，保留前导零；指数与个股使用不同实体类型。

#### T3.4 实现 5 年行情初始化

按股票或日期分片，保存 checkpoint。每批完成后校验 OHLC、负数、重复键、价格区间、成交量和交易日。单个证券失败进入 retry queue。

#### T3.5 实现财务、估值和板块导入

财务按报告期和公告日保存；板块成员为多对多；资金流保存供应商口径说明。无法可靠回填的字段写 unavailable 状态，不用 0 填充。

#### T3.6 实现 D1 发布器

使用参数化或经过严格转义的结构化批量写入，遵守 D1 的 100 个 bound parameters、100 KB SQL 和 30 秒查询限制；批次失败可重放，UPSERT 不产生重复事实。

**Gate 3**：选定样本完成端到端“抓取 -> staging -> 校验 -> D1 -> API”；重复运行数据行数不增加；故意中断后能续跑。

### Phase 4：因子、评分、候选和信号

#### T4.1 实现 Factor Registry

JSON 配置通过 schema 校验；字段、计算器、筛选器和显示层使用同一 key。增加因子不需要修改 Screener UI 条件组件。

#### T4.2 实现技术和风险因子

先实现收益、均线、斜率、RSI、量比、位置、波动率、回撤、Beta 和相对强度。滚动窗口必须按交易日且满足最小样本。

#### T4.3 实现基本面和估值因子

只使用 as-of 可见财务；负 PE 和缺失财务不参与有效权重；历史分位要求最小观测数，否则 unavailable。

#### T4.4 实现七维评分

横截面 percentile 使用固定 tie 方法；缺失因子按有效权重重新归一化；输出 `data_completeness`、`config_version` 和各维得分。

#### T4.5 实现 5 类候选

先执行 ST、退市风险、上市天数、停牌、成交额和完整度硬过滤，再按策略配置排名。理由只由结构化因子模板生成并保存原值和分位。

#### T4.6 实现市场信号

信号规则配置化，每日保存成员快照；交叉信号必须使用前一交易日值，不可用当天静态关系代替“上穿/下穿”。

#### T4.7 建立黄金样本测试

用固定小数据覆盖缺失财务、负 PE、停牌、ST、新股、拆分/分红、均线交叉、相同分位和极端值。

**Gate 4**：因子公式与手工结果一致；同一输入和配置版本重复计算完全一致；任何候选理由可追溯到保存值。

### Phase 5：同步编排与恢复

#### T5.1 实现目标交易日计算

使用 Asia/Shanghai 和 `MARKET_DATA_READY_TIME`；16:30 前取上一完整交易日，之后仅在日历为开市日时尝试当天。

#### T5.2 实现原子锁与租约

条件更新抢锁，设置 `lease_expires_at`；失败或 job 崩溃后可接管；保存 `sync_run_id`、GitHub run ID 和完整错误。

#### T5.3 实现 `POST /api/sync/check`

接口不接受目标日期；服务器自行查日历和完整日。无缺口返回 no-op，有运行中任务返回当前任务，仅新锁成功者调用 GitHub。

#### T5.4 实现 Actions 工作流

支持 schedule、workflow_dispatch、concurrency 和 timeout。Action 重新查询缺口，逐日执行行情、因子、评分、候选、信号和表现，最后才标完整。

#### T5.5 实现状态轮询

前端指数退避轮询；旧数据继续显示；成功后统一失效缓存并刷新。失败显示可操作错误，不无限轮询。

#### T5.6 故障演练

覆盖重复刷新、两并发请求、Provider 超时、部分股票失败、Action 被取消、锁过期、D1 批次失败和最后一步失败。

**Gate 5**：100 次并发 check 最多产生一个 workflow；失败日不标 complete；恢复后只补缺口。

### Phase 6：Screener 与基础页面

#### T6.1 实现 Screener repository

字段和排序使用白名单映射，值全部绑定参数。支持 AND/OR 第一层逻辑、分页、最大规则数、最大 limit 和查询超时保护。

#### T6.2 实现筛选 UI

控件完全由 Factor Registry 生成，数字单位明确，between/in 使用对应输入控件；模板保存在 localStorage 并带 schema version。

#### T6.3 实现导航、日期和状态框架

所有页面持续显示数据日期；全局搜索支持代码、名称和拼音首字母时再加入对应索引。

**Gate 6**：5,500 行 latest 数据下常用筛选 API 达到 P95 < 500 ms；SQL 注入用例全部失败；移动端输入不溢出。

### Phase 7：Dashboard、板块和信号

#### T7.1 市场概览

指数、涨跌家数、涨跌停和成交额使用同一完整交易日。涨跌停规则需按板块、ST 和价格舍入规则测试。

#### T7.2 热力图

Treemap 只传所需字段；面积值无效的股票单独处理。桌面测试全市场，移动端若性能不达标则按市值截取并明确显示覆盖比例。

#### T7.3 板块分析

行业和概念分别展示，成分股口径和资金流 `source_definition` 在 UI 可见；不使用“官方主力资金”表述。

#### T7.4 今日精选与市场信号

候选维度分、理由、负面因素和信号数量来自持久化快照；点击后进入带原条件的股票列表。

**Gate 7**：Dashboard 无数据、旧数据、同步中、失败和成功状态完整；1440px、768px、390px 无重叠；热力图交互流畅。

### Phase 8：个股详情与历史表现

#### T8.1 个股详情

实现不复权 K 线、成交量、复权指标线、七维评分条、财务、估值、板块、理由和历史评分。图表明确标注价格口径。

#### T8.2 候选不可变快照

候选主键包含日期、类型、代码和配置版本。后续可以补收益字段，但不得重算并覆盖当时排名、分数或理由。

#### T8.3 收益与基准

按交易日而非自然日补 1/5/20/60 日表现；标记停牌和不可成交；沪深 300 使用相同起止时点。

#### T8.4 表现统计

样本不足时显示样本数，不年化或外推；胜率、均值、中位数和超额收益按候选类型、配置版本分组。

**Gate 8**：用人工构造 10 日价格序列验证收益和 benchmark；未来日期未到时保持 pending，不写 0。

### Phase 9：安全、性能与运维

#### T9.1 安全加固

GitHub fine-grained token 仅目标仓库 Actions write；Cloudflare token 仅目标 D1 edit；日志脱敏；API 加限流、安全响应头和 body 大小限制。

#### T9.2 缓存与查询预算

完成日数据使用可缓存响应和 ETag；同步完成后按版本换 key。记录 D1 rows_read/rows_written，给每个关键 API 设置预算。

#### T9.3 备份与恢复

文档化 D1 Time Travel、定期 export、配置恢复和重建 latest 表。至少实际演练一次从备份恢复到测试数据库。

#### T9.4 可观测性

统一 run ID 串联 Worker、Action 和 pipeline；记录 provider 延迟、覆盖率、缺失数、D1 用量和同步耗时；错误包含可定位阶段。

**Gate 9**：安全测试、性能测试和恢复演练通过；故障手册能由未参与开发的人照着完成。

### Phase 10：部署与最终验收

#### T10.1 建立 Cloudflare 资源

创建三库、执行远端 migrations、配置 bindings、静态 Assets、Worker secrets 和环境变量。

#### T10.2 配置 GitHub

创建 Actions secrets、fine-grained token、concurrency、预算提醒和工作流权限；确认 workflow 位于默认分支。

#### T10.3 首次初始化

本地分片运行，逐批发布；每批生成校验报告。完成后执行全库完整性校验，再开放网站。

#### T10.4 生产冒烟测试

验证首页、搜索、筛选、板块、信号、个股、同步 no-op、真实缺口补齐和自动刷新。

#### T10.5 文档交付

README 包含本地启动、测试、迁移、初始化、日常同步、密钥轮换、数据口径、成本和故障恢复。

**Gate 10**：原需求 27 章的验收项逐条有证据；降级项明确标注，不能以“接口存在”替代真实运行证据。

## 8. 每个任务的执行格式

开发 AI 或工程师每次只领取一个任务编号，并按以下格式报告：

```text
任务：Tn.n
前置条件：已满足 / 未满足
本次范围：
修改文件：
关键决策：
运行命令：
测试结果：
数据证据：
遗留风险：
是否通过该任务验收：是 / 否
下一任务：Tn.n
```

执行规则：

1. 开始前阅读原需求、本任务书、相关 ADR 和当前 git diff。
2. 不覆盖用户已有修改，不把无关重构混入当前任务。
3. 先写失败测试或可验证样本，再实现行为。
4. Provider 单元测试可使用固定 fixture；最终运行链路禁止 Mock 数据。测试 fixture 不等于产品 Mock。
5. 所有数据任务都必须支持 dry-run、checkpoint 和幂等重跑。
6. 每个 Phase 结束运行全量 lint、typecheck、test、build；涉及 UI 时运行 Playwright 截图检查。
7. Gate 未通过时先修复或执行书面降级决定，不得继续堆叠后续功能。
8. 任何字段含义变化都要升级 schema/config version，并写 migration 或兼容策略。

## 9. 建议工期

对一名熟悉 TypeScript、Python 和 Cloudflare 的工程师，合理估算为 8–12 周：

| 阶段 | 预计工作日 |
| --- | ---: |
| Phase 0 可行性验证 | 4–7 |
| Phase 1–2 基础与契约 | 5–7 |
| Phase 3 数据链路 | 8–12 |
| Phase 4 因子与评分 | 7–10 |
| Phase 5 同步恢复 | 5–7 |
| Phase 6–8 产品功能 | 12–18 |
| Phase 9–10 上线加固 | 5–8 |

数据源异常、历史财务清洗和口径核对比 UI 编码更可能影响工期。任何要求“几天内一次生成全部项目”的计划都不能满足原文的数据正确性和可恢复性优先级。

## 10. 最终 Go / No-Go 条件

满足以下条件才进入完整开发：

- 接受个人、非商业研究用途及 AKShare 数据风险。
- 接受 Workers Paid 或经 Gate 0 验证后的替代存储方案。
- 核心行情 Provider 的覆盖率和补漏时间通过 Gate 0。
- 同意历史基本面在没有公告日证据时降级，不伪装成无偏历史评分。
- 同意先交付 5 年和上线后真实追踪，再按容量与数据质量扩展到 10 年。

若以上任一项不接受，应先更换数据源、存储或回测目标，再开始 Phase 1。
